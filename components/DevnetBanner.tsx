export function DevnetBanner() {
  return (
    <div className="w-full bg-yellow-300 text-yellow-950 text-center text-xs sm:text-sm font-mono py-1.5 px-3 border-b border-yellow-400">
      <span className="font-semibold">DEVNET</span> — test tokens only. No real
      money. Tips use devnet USDC on Solana devnet.
    </div>
  );
}
